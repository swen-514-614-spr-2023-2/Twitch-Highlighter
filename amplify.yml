version: 1
applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - echo "$PWD"
            - npm ci
        build:
          commands:
            - echo "$PWD"
            - IS_AWS_BUILD=${IS_AWS_BUILD}
            - VITE_IS_AWS_BUILD=${VITE_IS_AWS_BUILD}
            - echo "IS_AWS_BUILD=$IS_AWS_BUILD" >> .env
            - echo "VITE_IS_AWS_BUILD=$VITE_IS_AWS_BUILD" >> .env
            - npm run build
      artifacts:
        baseDirectory: dist
        files:
          - '**/*'
      cache:
        paths:
          - echo "$PWD"
          - node_modules/**/*
    appRoot: frontend/Twitchapp